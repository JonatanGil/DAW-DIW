import { IJSONResult } from './types';
/**
 * Parse the provided JSON returning a `JSONResult` with location information.
 * @param json The JSON string to parse
 * @param alternatePath The alternative `path` to use when one is not found
 */
export declare const parseJSON: (json: string, alternatePath?: string | undefined) => IJSONResult;
//# sourceMappingURL=parse-json.d.ts.map