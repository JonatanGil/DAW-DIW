import { ResourceType } from './enums';
/** Tries to load a module from `resourcePath`. */
export declare const tryToLoadFrom: (resourcePath: string) => any;
/**
 * Looks for a hint resource with the given `name` and tries to load it.
 * If no valid resource is found, it throws an `Error`.
 *
 * By default, the priorities are:
 *
 * 1. core resource
 * 2. `@hint/` scoped package
 * 3. `webhint-` prefixed package
 * 4. external hints
 *
 */
export declare const loadResource: (name: string, type: ResourceType, configurations?: string[], verifyVersion?: boolean) => any;
//# sourceMappingURL=load-resource.d.ts.map